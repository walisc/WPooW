language: php

php:
  - 5.4

script:
 - current_version=$(cat Build/version.txt)
 - echo $((current_version + 1)) > Build/version.txt

after_success:
 - git commit Build/version.txt  -m "Build Commit. Version numerb bumped"
 - git push git@github.com:walisc/wpAPI.git HEAD

notifications:
  email: false